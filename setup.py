# coding: utf-8
# create by tongshiwei on 2019/6/25

from setuptools import setup

setup(
    name='EduSim',
    version='0.0.1',
    install_requires=[
        'gym',
        'longling>=1.0.0',
        'tqdm',
        'networkx',
        'numpy'
    ]  # And any other dependencies foo needs
)
